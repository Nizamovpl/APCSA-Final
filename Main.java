public class Main{
    public static void main(String[] args){

        //this was not the plan but the only way I could solve an error is put everything into a non static function. 
        Menu menu = new Menu(); 
        menu.showMenu();
       
    }

}